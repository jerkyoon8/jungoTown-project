<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.juwon.springcommunity.repository.PaymentRepository">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `payments` (order_id, imp_uid, pay_method, amount, status, paid_at)
        VALUES (#{orderId}, #{impUid}, #{payMethod}, #{amount}, #{status}, NOW())
    </insert>

</mapper>
